# AUTHORITY_REQUIRED configuration — {{PRODUCT}}

Business, tax, legal and security values are never hard-coded. Read at runtime; unset → the operation
refuses with a CONFIG_NOT_SET error. Tests use a DEV_TEST seed; production values are set by an
authorised admin from current official sources.

| Key | Scope | Type | Used by | DEV_TEST seed |
|---|---|---|---|---|
